<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup | ChatApp</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e1e2f, #34346c);
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h2 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        .tagline {
            font-style: italic;
            color: #ccc;
            margin-bottom: 2rem;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.05);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.3);
            display: flex;
            flex-direction: column;
            gap: 1rem;
            width: 320px;
        }

        input {
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 6px;
            outline: none;
            background-color: #2c2c3c;
            color: white;
            transition: box-shadow 0.3s;
        }

        input:focus {
            box-shadow: 0 0 5px #00ccff;
        }

        button {
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            background-color: #4dff88;
            color: #1a1a1a;
            font-weight: bold;
            transition: transform 0.3s, background-color 0.3s;
        }

        button:hover {
            background-color: #2aff70;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <% if(locals.error) { %>
        <p><%= error %></p>
    <% } %>
    <h2>Create Your Account</h2>
    <div class="tagline">Let’s get you in the loop — your new chat era begins now 🚀</div>
    <form class="form-container" method="POST" action="/auth/signup">
    <input type="text" placeholder="Username" name="username" required />
    <input type="email" placeholder="Email" name="email" required />
    <input type="password" placeholder="Passcode" name="passcode" required />
    <button type="submit">Sign Up</button>
    </form>
</body>
</html>
