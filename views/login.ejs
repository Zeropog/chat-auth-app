<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | ChatApp</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e1e2f, #34346c);
            color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h2 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.05);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.3);
            display: flex;
            flex-direction: column;
            gap: 1rem;
            width: 300px;
        }

        input {
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 6px;
            outline: none;
            background-color: #2c2c3c;
            color: white;
            transition: box-shadow 0.3s;
        }

        input:focus {
            box-shadow: 0 0 5px #ffcc00;
        }

        button {
            padding: 10px;
            font-size: 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            background-color: #ff4d6d;
            color: white;
            transition: transform 0.3s, background-color 0.3s;
        }

        button:hover {
            background-color: #ff1f4c;
            transform: scale(1.05);
        }

        .tagline {
            margin-bottom: 2rem;
            font-style: italic;
            color: #ccc;
        }
    </style>
</head>
<body>
        <% if(locals.error) { %>
        <p><%= error %></p>
         <% } %>
    <h2>Login to ChatApp</h2>
    <div class="tagline">Welcome back, let's get you connected 💬</div>
    <form class="form-container" method="POST" action="/auth/login">
    <input type="text" placeholder="Email" name="email" required />
    <input type="password" placeholder="Passcode" name="passcode" required />
    <button type="submit">Login</button>
    </form>
</body>
</html>
